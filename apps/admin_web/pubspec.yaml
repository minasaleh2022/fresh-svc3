name: fresh_admin_web
publish_to: none
environment: { sdk: '>=3.5.0 <4.0.0' }
dependencies:
  flutter: { sdk: flutter }
  go_router: ^14.2.0
  intl: ^0.19.0
  shared: { path: ../../shared }
flutter:
  uses-material-design: true
  assets: [ assets/logo_fresh.webp ]
